{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "install": "npm --prefix resources/js install",
    "dev": "npm --prefix resources/js run dev",
    "build": "npm --prefix resources/js run build"
  }
}
